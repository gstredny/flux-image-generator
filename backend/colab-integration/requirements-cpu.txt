# CPU-optimized requirements for Codespaces
# Install PyTorch CPU version to reduce download size and memory usage

# Core dependencies (lightweight)
fastapi==0.110.0
uvicorn[standard]==0.27.1
pydantic==2.6.1
pillow==10.2.0
python-multipart==0.0.9
nest-asyncio==1.6.0
pyngrok==7.1.3

# PyTorch CPU version (much smaller than CUDA version)
--index-url https://download.pytorch.org/whl/cpu
torch==2.2.0+cpu
torchvision==0.17.0+cpu

# Diffusers and transformers (after PyTorch)
diffusers==0.26.3
transformers==4.38.1
accelerate==0.27.2